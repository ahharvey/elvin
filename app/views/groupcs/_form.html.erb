<%= form_for(@groupc) do |f| %>
  <% if @groupc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@groupc.errors.count, "error") %> prohibited this groupc from being saved:</h2>

      <ul>
      <% @groupc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :order %><br />
    <%= f.text_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
