<%= form_for(@groupc_user) do |f| %>
  <% if @groupc_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@groupc_user.errors.count, "error") %> prohibited this groupc_user from being saved:</h2>

      <ul>
      <% @groupc_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :groupc %><br />
    <%= f.text_field :groupc %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
