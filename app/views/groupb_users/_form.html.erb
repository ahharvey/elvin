<%= form_for(@groupb_user) do |f| %>
  <% if @groupb_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@groupb_user.errors.count, "error") %> prohibited this groupb_user from being saved:</h2>

      <ul>
      <% @groupb_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :groupb %><br />
    <%= f.text_field :groupb %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
